"""
Guía de instalación para PostgreSQL
===================================

1. INSTALAR POSTGRESQL:
   - Descargar desde: https://www.postgresql.org/download/windows/
   - Durante la instalación, recordar:
     * Puerto: 5432 (por defecto)
     * Usuario: postgres
     * Contraseña: admin (o la que prefieras)

2. INSTALAR DEPENDENCIAS DE PYTHON:
   Ejecutar estos comandos en la terminal:
   
   pip install psycopg2-binary
   pip install pillow
   pip install matplotlib
   pip install tkcalendar

3. VERIFICAR INSTALACIÓN:
   - Abrir pgAdmin (interfaz gráfica de PostgreSQL)
   - Conectar con usuario 'postgres' y tu contraseña
   - Crear base de datos llamada 'hospital_db'

4. CONFIGURAR CONEXIÓN:
   - Editar archivo modelo/database.py si necesitas cambiar credenciales
   - Por defecto usa: host=localhost, user=postgres, password=admin
"""
